# Open HMI Network V0.2.2

This update applies one UX rule across the site: one decision per page/step, short labels, one-line explanations, and a consistent Find → Evaluate → Build → Scale funnel.

Upload all files in this folder to the root of the existing GitHub repository and commit to `main`.
